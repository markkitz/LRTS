const React = require('react');
const {connect} = require('react-redux');
import CourseListing from '../containers/Courses/CourseListing'

let CourseListingView = () => {
  return (
          <CourseListing />
        )
}
export default  CourseListingView
