const React = require('react');
import SubForm  from '../../../components/Subform'
import TextField from 'material-ui/lib/text-field';

const CoursePackCopyrightForm =   ({ model, onPropertyChange}) =>
      {
        let form ="CoursePackCopyrightForm";
        let actionType = "UPDATE_COPYRIGHTFORM"


          return (
          <SubForm form={form} >
            <h1>test  123</h1>
          </SubForm>);
      };

export default CoursePackCopyrightForm;
